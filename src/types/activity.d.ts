type UfoActivity = {
    _id: number
    name: string
    nb_fields: number
    nb_teams: number
    category: string
}

type UfoNewActivity = {
    name: string,
    nb_fields: number,
    nb_teams: number,
    category: string
}
